CREATE TABLE `STUDENT` (
 `STUDENT_ID` int(4) NOT NULL auto_increment,
  `STUDENT_NAME` varchar(50) NOT NULL,
  `STUDENT_AGE` INTEGER(15),
  `STUDENT_COURSE` varchar(100) NOT NULL,
  PRIMARY KEY  (`STUDENT_ID`)
);